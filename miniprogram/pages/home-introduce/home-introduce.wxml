<view class="page-poster">
  <view>
    <van-image
      wx:for="{{imgList}}"
      wx:key="index"
      show-loading="{{false}}"
      width="100vw"
      height="673px"
      src="{{item}}"
      lazy-load
    />
  </view>
</view>

